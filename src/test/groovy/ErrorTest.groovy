import com.example.demo.CustomersAndUsers.Customer
import org.apache.camel.component.mock.MockEndpoint
import org.junit.Test
import spock.lang.Specification

class ErrorTest extends Specification {
  /*      MockEndpoint ErrorEndpoint



    @Test
    public void testInvalidDataFailure() throws Exception {
        given:
        ErrorEndpoint.expectedBodiesReceived(
        // the failure processor will enrich the message and set a header with a constructed error message
        ErrorEndpoint.expectedHeaderReceived("FailureMessage", "The message failed because noUserMatch")

        when:
        template.sendBody("direct:start", )

        then:
        ErrorEndpoint.assertIsSatisfied()
    }*/
}
